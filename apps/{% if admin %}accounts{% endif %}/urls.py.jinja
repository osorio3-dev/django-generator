"""URL configuration for the accounts app.

Most auth routes are provided by allauth.urls (mounted in config/urls.py).
This file is reserved for app-specific custom routes (autologin, profile,
password reset, etc.).
"""

from django.urls import path

urlpatterns: list[path] = []