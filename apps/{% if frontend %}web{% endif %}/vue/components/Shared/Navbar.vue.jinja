{% if frontend and frontend_style == 'vue-spa' %}
<template>
  <header class="bg-white border-b border-gray-200 dark:bg-neutral-800 dark:border-neutral-700">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16 items-center">
        <router-link to="/" class="text-xl font-semibold text-gray-800 dark:text-white">
          {{ brand }}
        </router-link>
        <nav class="flex items-center gap-x-3">
          <router-link
            to="/"
            class="text-sm text-gray-700 hover:text-blue-600 dark:text-neutral-300 dark:hover:text-blue-400"
            active-class="text-blue-600"
            exact-active-class="text-blue-600"
          >
            Inicio
          </router-link>
          <router-link
            to="/dashboard"
            class="text-sm text-gray-700 hover:text-blue-600 dark:text-neutral-300 dark:hover:text-blue-400"
            active-class="text-blue-600"
          >
            Dashboard
          </router-link>
        </nav>
      </div>
    </div>
  </header>
</template>

<script setup>
defineProps({
  brand: { type: String, required: true },
});
</script>
{% endif %}
