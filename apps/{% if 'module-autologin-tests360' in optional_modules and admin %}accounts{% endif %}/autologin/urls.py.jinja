"""AutoLogin URL config — only mounted when module-autologin-tests360 is active."""

from django.urls import path

from apps.accounts.autologin.views import autologin, autologin_confirm

urlpatterns = [
    path("autologin/<str:token>/", autologin_confirm, name="autologin-confirm"),
    path("autologin/<str:token>/redeem/", autologin, name="autologin"),
]
