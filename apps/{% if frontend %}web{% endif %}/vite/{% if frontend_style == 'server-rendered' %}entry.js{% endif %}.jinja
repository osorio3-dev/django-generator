// Server-rendered Vite entry — imports Tailwind v4 CSS + HTMX + Alpine + Preline.
// Vite reads this file (see vite.config.ts) and outputs the manifest to static/dist/.

import './style.css';
import 'htmx.org';
import Alpine from 'alpinejs';
import collapse from '@alpinejs/collapse';
import 'preline';

window.Alpine = Alpine;
Alpine.plugin(collapse);
Alpine.start();

// Re-initialize Preline after every HTMX swap so dropdowns, modals, and other
// components bind correctly to freshly inserted DOM nodes.
document.body.addEventListener('htmx:afterSwap', () => {
  if (window.HSStaticMethods && typeof window.HSStaticMethods.autoInit === 'function') {
    window.HSStaticMethods.autoInit();
  }
});
